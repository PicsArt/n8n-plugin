# 📌 Picsart AI Image Workflow  **Generate, Enhance, Remove Background & Save to Google Drive**This workflow connects **Google Sheets**, **Picsart AI**, and **Google Drive** to automate image generation and image processing.---## ✅ Why This Workflow WorksThis workflow is designed to be **data-driven and fully automated**:1. **All tasks are stored in a Google Sheet**     You can add, update, or delete tasks without changing the workflow.2. **A Switch node routes tasks based on operation type**     Supported operations:   - `text2image`   - `upscale`   - `removeBackground`3. **Picsart API performs the AI image processing**     Picsart provides AI features for generating and editing images.4. **Outputs are saved automatically to Google Drive**     Processed images are stored in a specific folder for easy access.---## 🔧 How It Works (Step-by-Step)### 1️⃣ Trigger + Read Google Sheet- Workflow starts manually or using a schedule trigger.- **Google Sheets node** reads rows from the sheet.### 2️⃣ Route Operation- The **Switch node** checks the `operation` column.- Routes the task into the correct branch:| Operation | Branch ||----------|--------|| `text2image` | Generate AI image || `upscale` | Upscale existing image || `removeBackground` | Remove background |### 3️⃣ Run Picsart OperationEach branch uses the **Picsart node**:| Operation | Input | Output ||----------|-------|--------|| **text2image** | `prompt` | generated image || **upscale** | `imageUrl` + `factor` | enhanced image || **removeBackground** | `imageUrl` | transparent background image |### 4️⃣ Save Result to Google DriveThe final image is saved automatically to the folder specified in the **Google Drive node**.---## ⚙️ Setup Instructions### 1️⃣ Create Google SheetCreate a Google Sheet with these columns:| Column | Description ||--------|-------------|| `prompt` / `imageUrl` | Text prompt for text2image or image URL for other operations || `operation` | `text2image`, `upscale`, or `removeBackground` || `factor` | Upscale factor (2, 4, 6, 8) |Example:| prompt / imageUrl | operation | factor ||-------------------|-----------|--------|| "sunset city" | text2image | - || https://...jpg | upscale | 4 || https://...png | removeBackground | - |---### 2️⃣ Add Picsart API Key- Go to **Picsart API dashboard**- Generate API Key- Add it to n8n as a credentialDocs: https://docs.picsart.io/docs/creative-apis-get-api-key---### 3️⃣ Add Google Credentials- Add Google Sheets and Google Drive credentials in n8n- Use **OAuth** or **Service Account**Docs: https://docs.n8n.io/integrations/builtin/credentials/google/oauth-single-service/---### 4️⃣ Set Google Drive Folder- In the Google Drive node, select your target folder    Output images will be saved here automatically.---## 🧠 Notes### 🔹 Input rules- **text2image** → uses `prompt`- **upscale** → uses `imageUrl` + `factor`- **removeBackground** → uses `imageUrl`